get:
  summary: Етапи документів
  operationId: getDocStages
  tags:
    - dictionaries
  parameters:
    - $ref: ../components/parameters/RequestId.yaml
  security:
    - bearerAuth: []
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            description: OK
            type: array
            items:
              $ref: ../components/schemas/dictionaries.DocStage.yaml
    "401":
      $ref: ../components/responses/401Unauthorized.yaml
